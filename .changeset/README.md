# Changesets in Angora

Welcome! This folder contains Changesets configuration and pending changesets.

- To generate a new changeset: run `bun run changeset`
- To bump package versions and update changelogs: run `bun run version-packages`
- To publish changed packages to npm: run `bun run release`
